// Enable Ajax loading of View templates
jQuery(document).ready(function($) {
  // We can also pass the url value separately from ajaxurl for front end AJAX implementations
//  jQuery('a.date-link').click(function() {

//     var data = {
//       action:       'my_action',
// //      postid:       jQuery(this).data('postid'),
// //      viewtemplate: jQuery(this).data('viewtemplate'),
//       starttime:    jQuery(this).data('starttime'),
//       endtime:      jQuery(this).data('endtime'),
//       wrapper:      jQuery(this).data('wrapper'),
//     };

// //    jQuery.post(ajax_object.ajax_url, data, function(response) {

//          console.log(data);

// //      console.log( data.starttime );
// //      console.log( data.endtime );
// //      console.log( data.wrapper );

// //      jQuery('.slider-for').slick('unslick');
//       jQuery(data.wrapper).html(response);
// //      jQuery('.slider-for').slick('reinit');

    // });

//  });

});
